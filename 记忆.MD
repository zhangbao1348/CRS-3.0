# 项目记忆

## 2026-08-24：项目全量扫描初始化

- 用户要求扫描并确认整个 CRS 演示项目的内容。
- Graphify 检测到工作区包含 450 个受支持文件、约 547,486 词：423 个代码文件、7 个文档、20 个图片；另有 3 个敏感文件被自动跳过。
- 项目为 React + Vite 前端（`src/`，含酒店、价格、库存、渠道、预订、报表、集团、系统设置等页面模块）与 Maven 后端（`backend/`）的 CRS 系统；PRD 位于 `.kiro/specs/prd/`，历史经验位于 `.kiro/memory/lessons/`。
- 由于语料超过 Graphify 的 200 文件安全阈值，完整语义图谱需用户确认扫描范围后继续；尚未修改业务代码。

## 2026-08-24：本地一键启动脚本方案待确认

- 用户希望参考 `/Users/willawang/Desktop/我的项目/新逍遥项目`，为 CRS 项目提供本地一键启动脚本。
- 已确认新逍遥的 `restart-all.sh` 具备前置校验、构建完成后再切换端口、日志托管、HTTP 健康检查与最终状态汇总等模式。
- CRS 当前由 Vite 前端（端口 3001）和 Spring Boot 后端（端口 8080）组成，后端依赖本机 MySQL 的 `CRS` 数据库；本机已检测到 Java、Maven、Node、npm、MySQL、lsof、curl。
- 当前工作区已有用户未提交的业务代码与 PRD 改动，后续仅新增启动脚本，不触碰这些文件。根据 SOP，待用户确认 RFC 后再写入。
- 用户进一步确认期望：脚本应以单条命令完成本地前后端启动，不依赖人工分开构建或打开多个终端；仍等待明确的 `Proceed` 指令。

## 2026-08-24：本地一键启动脚本已实现

- 已新增项目根目录 `start-local.sh`：一条命令检查本机依赖与 MySQL、按需安装前端依赖、构建 Spring Boot JAR、启动前后端，并以 HTTP 健康检查确认就绪。
- 脚本使用 `logs/` 与 `pids/` 保存运行状态；`--restart` 和 `--stop` 只操作其自身记录的进程，端口被未知进程占用时会中止而不强杀。
- 已同步 `.kiro/specs/prd/00-SOW-功能清单.md`，标记本地开发启动能力为已实现。`bash -n`、帮助命令与本次文件空白检查均已通过，脚本权限为 `755`；本机未安装 ShellCheck，且未实际启动服务，以免占用端口或改动运行状态。
- Graphify 增量检测发现既有清单缺失，因而将全部 451 个文件判定为待处理；按超过 200 文件的安全阈值，需用户指定扫描范围后再重建图谱。
- 首次实际启动发现 `mysqladmin ping` 在数据库需要认证时会误报不可用；已改为检查 MySQL 是否监听本机 `3306`，实际数据库认证与可用性仍由后端 `/actuator/health` 验证。
- 第二次启动发现 Bash 在中文全角右括号紧邻变量名时会将其解析为变量名的一部分；后端、前端启动提示已改为 `${变量名}` 形式，避免受本机 locale 影响。
- 用户授权实际启动后，`./start-local.sh` 已在本机成功完成构建与启动：后端 `http://localhost:8080/actuator/health` 于约 19 秒通过，前端 `http://localhost:3001` 于约 2 秒通过。运行日志位于 `logs/`，可用 `./start-local.sh --stop` 停止。
- Codex 临时执行会话结束后会清理其子进程，因此服务必须由独立的 macOS Terminal 托管。已通过 Terminal 启动后再次在宿主环境核验：后端 `8080`、前端 `3001` 均在监听，后端健康接口返回 `200 {"status":"UP"}`，前端根路径返回 `200 OK`。受限执行环境与浏览器自动化策略无法直接访问该 Terminal 托管的 localhost，不代表宿主服务不可用。

## 2026-08-24：登录页视觉升级方案待确认

- 用户认为当前登录页过于低级，并提供截图作为现状参考；截图仅作为视觉输入，不包含需执行的指令。
- 已使用 `frontend-design` 与 `ui-ux-pro-max` 技能审视现有 `src/pages/Auth/Login.jsx`、`Login.css`。问题是标题、英文副标题、三组胶囊标签、演示账户与功能标签同时堆叠，视觉层级松散且卡片过于纵向冗长。
- 拟改为“酒店经营操作室”风格：暖白工作台背景、克制的深墨品牌面、左右双栏且移动端收敛为单栏；左侧只保留可信任的运营叙事与简洁的状态模块，右侧聚焦登录表单。认证接口、路由和记住我逻辑不变；演示账号将弱化为非主视觉信息。
- 用户要求以 UI 组件完善页面：将复用项目既有 Ant Design 的 `Form`、`Input`、`Checkbox`、`Button`、`Card`、`Tag`、`Divider`、`Statistic` 等组件，并通过 CSS token 与布局组合形成统一界面，避免新增第三方依赖。
- 用户进一步明确：实现必须遵循已安装的 UI Skills；已选择并读取 `frontend-design`（品牌化、高完成度视觉方向）与 `ui-ux-pro-max`（设计系统、响应式、可访问性与表单反馈规范）。
- 用户已确认 `Proceed`；已重构 `src/pages/Auth/Login.jsx` 与 `Login.css`。页面使用 Ant Design 组件搭建深墨品牌侧栏 + 暖白认证工作台；保留 AuthContext 认证、路由、记住我与演示账号信息，新增可见焦点、错误层级、375px 收敛布局和 `prefers-reduced-motion` 支持。同步更新 `.kiro/specs/prd/09-系统设置.md` 的登录体验实现状态。
- 验证完成：`npm run build`、登录文件定向 ESLint、变更空白检查均通过；项目全仓 Lint 仍有 403 项既有错误。Playwright 已在 1440px 与 375px 宽度完成渲染验收，登录工作台、可见表单标签和移动端收敛布局均正确。已运行 `graphify update .` 更新代码图谱（4,533 节点、5,049 边、405 社区），并写入登录页 UI 重构经验记录。
- 用户在实机截图中发现已填充用户名和密码视觉上未上下居中；已为 Ant Design 输入框内部文字、前后缀图标设定与 52px 控件一致的 50px 高度和行高，待以同尺寸自动填充截图复验。
- 已以 510×788 CSS 视口、2 倍像素比复现用户截图尺寸并填充演示账号；账号、密码、前缀图标及密码可见按钮均已垂直居中。首次截图左侧露出的深灰“模板”浮层未出现在登录页的独立 DOM 渲染中，判断为页面外层或浏览器浮层。修复后 `npm run build` 和 `graphify update .` 均通过。

## 2026-08-25：基于 PRD 的整体代码结构审计

- 用户要求依据 `.kiro/specs/prd/` 的整体需求扫描代码结构是否需要优化；本轮只做审计，没有修改业务代码。
- 当前图谱为 4,533 节点、5,049 边、405 社区；核心高连接节点集中在 `Reservation`（114 边）、`RatePlan`（81 边）、`GroupRateCode`（75 边）、`Hotel`（65 边），订单、价格与集团房价码是优先拆分边界。
- 前端约 42,247 行，存在多个超大页面：`AddGroupRateCode.jsx` 2,432 行、`EditHotel.jsx` 1,973 行、`AddHotel.jsx` 1,611 行、`AddRatePlan.jsx` 1,318 行；路由、菜单、主布局分别为 443/587/630 行。后端约 38,781 行，`OpenReservationController` 1,332 行、`GroupRateCodeController` 1,093 行、`OpenHotelController` 1,028 行。
- 后端仍以 controller/service/entity/repository 横向分层（56/68/71/68 个类），与 PRD 按订单、库存、价格、渠道、集团等业务域组织的方式不一致；建议渐进迁移为按业务域分包，域内再分 api/application/domain/infrastructure。
- P0 风险：`SecurityConfig` 对 `/api/**` 和其余请求全部 `permitAll`；数据库密码与 JWT 密钥以明文存在默认配置；41 个 SQL 文件未采用 Flyway/Liquibase 的标准版本目录和命名，并同时启用 `ddl-auto=update`。
- 前端数据访问未统一：14 个文件直接导入 axios，`api.js` 已达 540 行；多个页面仍硬编码 `tenantId/groupId = 1`，并存在演示数据或 localStorage 兜底。PRD 也明确标注渠道映射为“部分实现、使用模拟数据”。
- 质量门禁不足：全仓仅 4 个测试文件；ESLint 扫描 116 个文件，87 个文件含错误，共 403 errors/49 warnings。前后端编译均通过，但 Vite 主 JS 包约 4.33 MB（gzip 1.31 MB），需要路由懒加载和分包。
- PRD 功能覆盖并非全部闭环：订单 Phase 2-4 仍有后台订单、退款/促销、回调通知与 PMS 同步事项；PMS PRD 覆盖价格、库存、订单双向生命周期，但当前专用 PMS 代码主要集中在库存；渠道映射仍为模拟数据。
- 建议顺序：P0 先修鉴权/密钥/迁移；P1 建立按域目录和统一 API/DTO 边界并拆超大控制器/页面；P1 同步补租户隔离与移除 Mock；P2 增加核心订单库存契约测试、路由懒加载和统一质量门禁。避免一次性重写。
- 用户确认是否需要推倒重来；结论是不需要。现有 React/Vite、Spring Boot、MySQL 技术栈和主要业务模块可继续使用，采用“保持可运行、按业务域逐步替换”的渐进式重构策略即可。

## 2026-08-25：代码结构优化与技术债治理 RFC 待确认

- 用户要求制定完整计划解决结构审计发现的问题。
- 已新增 `.kiro/specs/prd/18-代码结构优化与技术债治理计划.md`，状态为 Proposed；计划明确不换技术栈、不拆微服务、不一次性重建数据库，采用模块化单体和前端 feature-based 的渐进迁移。
- RFC 分为 8 个阶段：基线冻结；安全/配置/迁移；架构底座；订单与库存试点；价格/集团/酒店；渠道/PMS/Mock 清理；前端性能；报表诊断与 PRD 闭环。
- 每阶段均定义任务、验收标准和回滚策略，并设置统一质量门禁：前后端构建/测试、Lint、API 契约、数据库迁移、UI 验收、Graphify、PRD 和经验记录同步。
- 推荐 6 个迭代、每个迭代约 1-2 周；首轮结构治理粗估 35-55 人日，不含真实 PMS/CRM 厂商联调等待。
- 按项目 SOP，本轮仅形成 RFC，没有修改业务代码；等待用户回复 `Proceed` 后从阶段 0 的基线和安全网建设开始。

## 2026-08-25：治理执行启动与阶段 0 基线建设

- 用户授权按 RFC 节奏自主执行，只有不确定或高风险事项再确认；随后明确要求后续不得修改 PRD。所有 PRD 自此只读，实施记录改存 `.kiro/baseline/` 和 `记忆.MD`。
- 阶段 0 新增独立基线目录 `.kiro/baseline/2026-08-25/`，包含质量基线、核心烟雾测试矩阵、PRD—代码只读追踪矩阵、数据库备份恢复方案和阶段报告；没有修改现有 PRD。
- 修复测试基础设施：ESLint 识别 Jest/Node 环境；库存测试补齐 API/Ant Design Mock 并正确等待异步 Effect；前端 2 suites / 34 tests 全部通过，测试文件定向 ESLint 通过。
- `ReportInitTest` 实际是会写数据库的报表初始化工具，已标记 `@Disabled` 防止自动测试修改数据。Maven 必须显式使用 Java 17；本机默认 Maven 使用 Java 25 会导致 Mockito/Byte Buddy 自附加失败。
- 新增 `scripts/check-eslint-baseline.mjs` 和 `scripts/verify-baseline.sh`。全套验证通过：前端构建、34 个测试、ESLint 冻结阈值、Java 17 后端编译和测试。当前 ESLint 基线为 266 errors / 49 warnings，只允许减少不得新增。
- 数据库只读盘点：MySQL 9.6.0、68 张表、260,063 条订单；结构 SHA-256 为 `74d51ffc94ee1229ee351b68b51b4fa8d5edbc6dbd3746f69f8cb86cafc9d8cc`。进入 Flyway 阶段前必须先确认持久备份目录并完成恢复演练。
- 已执行 `graphify update .`，代码图谱更新为 4,534 节点、5,050 边、405 社区。阶段 0 当前仅剩数据库持久备份与恢复演练位置待用户确认。

## 2026-08-25：阶段 0 数据库备份恢复演练完成

- 用户回复“继续”，同意使用默认持久备份目录 `/Users/willawang/CRS-backups/2026-08-25/`；本轮继续遵守“不得修改 PRD”的要求。
- 已生成 schema-only 与 full dump，显式关闭 GTID 写入；文件大小分别为 94,243 和 261,612,347 字节，权限均收紧为 `600`，未放入项目或 Git。
- schema 备份 SHA-256 为 `bae56036144078dcb23f784ed232bf8d0f4a3c020d972cf0c8b7cc4ec52592c4`；完整备份 SHA-256 为 `b9da03d0a1a45196cc8c6b7e954e9e7c3a6202d7fd8fe72f8ff880f2432cd83d`。
- 完整备份已两次导入唯一临时库 `CRS_restore_verify_20260825`：68 张表、结构指纹及 9 个关键表行数与原库完全一致。
- 后端使用 Java 17、`ddl-auto=validate` 和临时端口 18080 连接恢复库启动，健康接口返回 `UP`。随后临时实例和临时数据库均已清理，原 `CRS` 库未覆盖、未删除。
- 阶段 0 至此完成；下一步进入阶段 1 的密钥外置、默认安全配置和认证/租户边界治理。

## 2026-08-25：阶段 1.1 敏感配置与登录日志治理

- 所有 PRD 继续保持只读。本轮只修改运行配置、一键脚本、认证日志与 JWT 工具，并将记录写入 `.kiro/baseline/` 和 `.kiro/memory/lessons/`。
- Spring 跟踪配置已移除数据库密码、JWT 密钥和固定生产数据库地址；新增 `.env.example`，本机真实值位于被忽略且权限为 `600` 的 `.env.local`。
- `start-local.sh` 改为安全逐行解析 `.env.local`，校验必填变量并固定 Java 17。没有使用 `source`，避免 JDBC URL 的 `&` 被 Shell 解释或环境文件内容被执行。
- 本机 JWT 已生成新随机密钥；`JwtUtil` 使用 `SecretKey`，启动时拒绝少于 32 字节的密钥。新增 2 个 JWT 单元测试，全部通过。
- `AuthController` 已删除明文密码、密码哈希和请求体调试输出，对外响应不再泄露内部异常。进一步发现 Spring DEBUG 会自动记录登录 JSON，已将框架默认日志降为 INFO，复验日志敏感扫描通过。
- 独立端口 18081 的新配置后端健康检查返回 `UP`，演示账号登录契约通过；全套基线验证通过：前端 34 tests、ESLint 266/49 冻结线、Java 17 后端测试均正常。
- Graphify 更新为 4,539 nodes / 5,054 edges / 406 communities。
- 用户随后明确批准创建专用 MySQL 账号。已创建 `crs_app@localhost`，仅授权 `CRS.*` 上的 `SELECT, INSERT, UPDATE, DELETE, CREATE, DROP, INDEX, ALTER, EXECUTE`；全局仅 `USAGE`，没有远程来源或其他数据库权限。
- `.env.local` 已切换到 `crs_app` 和满足本机复杂度策略的随机密码，MySQL root 密码未修改。使用专用账号的 18081 临时后端健康、登录契约与敏感日志扫描均通过，临时实例及两份一次性密码文件已清理。

## 2026-08-25：阶段 1.2 API 鉴权与租户边界治理

- 所有 PRD 继续只读。本轮收紧 Spring Security 默认策略：仅匿名开放登录/刷新、开放 API、Swagger 和 health/info，其余 `/api/**` 必须 JWT；租户、注册、权限初始化和测试权限接口限制为 `ROLE_super_admin`。
- `UserDetailsServiceImpl` 已从 `user_roles/roles` 加载有效角色；禁用用户不会再凭旧 JWT 建立认证上下文。
- 租户拦截器取消“请求头无条件覆盖”和“默认租户 1”逻辑：普通用户只能使用 JWT 租户，跨租户头返回 403；超级管理员可显式选择租户，未选择时返回 400。
- 为兼容 13 个历史直接 axios 页面，默认 axios 与统一 API 实例现在共享 token、租户和 trace 请求头；TenantProvider 改为登录后加载租户并同步持久化首次选择。
- 新增前端请求上下文测试 2 个、后端租户边界测试 4 个。全套结果：前端 3 suites / 36 tests，后端 6 个有效测试全部通过，1 个手工初始化测试跳过；ESLint 降为 265 errors / 48 warnings。
- 18082 真实烟雾矩阵通过：匿名业务 API 401、开放 API 缺密钥 401、匿名注册 401、敏感 actuator 401、登录 200、超级管理员租户列表 200、缺租户 400、租户 1/2 切换均 200。临时实例已清理。
- Graphify 更新为 4,553 nodes / 5,076 edges / 408 communities。
- 下一步进入资源级授权和 Flyway baseline：逐模块审计裸 ID 查询/用户代查，并在迁移稳定后移除 `crs_app` DDL 权限、把开发环境切换为 `ddl-auto=validate`。

## 2026-08-26：阶段 1.2 正式服务切换与启动脚本加固

- 已尝试用独立 macOS Terminal 执行 `start-local.sh --restart`，使新鉴权代码、随机 JWT 密钥和 `crs_app` 数据库账号在长期运行的 8080/3001 服务上生效。
- 首次失败因 Bash `set -u` 把中文全角括号紧邻的 `$label` 解析为错误变量名；已将相关变量统一改为 `${label}`、`${pid}`、`${port}` 明确边界。
- 第二次发现旧 Spring Boot 进程优雅关闭约需 60 秒，原 15 秒超时导致前端已停而启动流程中断。脚本现等待最多 90 秒，仍只处理 PID 文件与端口一致的进程，不自动强杀。
- 第三次重启成功：新后端 PID `82355`、新前端 PID `82846`；8080 health 与 3001 根路径均返回 200。
- 正式 8080 验收通过：匿名业务 API 401，管理员登录 200，租户列表 200，租户 1 酒店查询 200，后端日志敏感模式扫描通过。
- Graphify 复核为 4,553 nodes / 5,076 edges / 408 communities。所有 PRD 文件继续保持未改动。

## 2026-08-26：阶段 1.3 P0 资源级授权与跨租户 ID 治理

- 所有 PRD 继续只读。本轮新增 `ResourceAuthorizationService`，将用户菜单/权限查询收紧为“本人或超级管理员”；用户、角色、菜单管理与无租户字段的旧操作日志限制为 `ROLE_super_admin`。
- 订单、库存、酒店、房型、价格计划、酒店图片、租户渠道和档案的高风险裸 ID 已改为数据库级 `id + tenantId/groupId` 查询。价格计划权限接口、同酒店编码房型误放行、图片请求体篡改归属和开放接口档案跨集团查询均已修复。
- 新增 5 个资源授权单元测试和 3 个房型租户边界测试。Java 17 后端全量测试、前端 36 测试、前端生产构建全部通过；ESLint 保持 265 errors / 48 warnings。
- Java 17、`ddl-auto=validate`、18083 临时实例启动成功，68 个仓储全部初始化。实时矩阵通过：匿名管理接口 401，普通用户管理/日志 403，本人菜单 200，他人菜单 IDOR 403，超级管理员 200。
- 实时矩阵使用的固定命名临时普通用户已显式删除，残留数确认为 0。经验记录了 MySQL 多表删除必须显式选库，且不能只依赖静默 `trap`。
- Graphify 更新为 4,590 nodes / 5,140 edges / 411 communities。控制器/服务仍有 68 处 `findById`，下一批按图谱高连接度审计集团房价码、集团房型、码表/政策和渠道映射，而不一次性重写。

## 2026-08-26：阶段 1.3D 业务资源裸 ID 治理收口

- 所有 PRD 继续只读。集团房价码/房型、差价、政策/码表、渠道映射、酒店分配、酒店设施、基础价格等业务资源已改为数据库级 `id + tenantId/groupId` 组合查询。
- 修复 3 类实际写越权：房型差价 weekdays 裸 ID 更新、酒店房型分配用其他租户主键覆盖/删除、酒店设施直接保存请求对象/`deleteById`/`findAll`。
- 酒店房型分配和酒店设施更新改为读取当前租户现有实体后白名单复制字段，归属字段不允许随请求移动；集团房型的 CODE 下发与停用也补齐集团/租户条件。
- 剩余裸 ID 已完成分类：用户/角色/菜单/租户/操作日志属于超级管理员域；Group 主键即租户 ID；无 tenant_id 的 GroupFacility 是共享目录，写操作已限制超级管理员。
- 新增 6 个租户边界测试。全套基线通过：前端 36 tests，ESLint 265/48，后端 21 tests run、0 failures、0 errors、1 skipped。
- Java 17、`ddl-auto=validate`、18084 临时实例成功解析 68 个 Repository，健康检查返回 UP，临时实例已停止。
- Graphify 更新为 4,634 nodes / 5,217 edges / 413 communities。阶段 1.3 完成，下一步进入 Flyway baseline 与迁移治理。

## 2026-08-26：阶段 1.4 Flyway 底座完成，正式 baseline 待明确批准

- PRD 继续只读。后端引入 Spring Boot 管理的 Flyway 9.22.3，新增 `V1__baseline_schema.sql`（68 张业务表，无数据，SHA-256 `a5f8bcab9f97d3eab58605b4b9b6c6f0fb1e3364499624ee2cc54454229218c5`）。
- Hibernate 默认/dev 从 `ddl-auto=update` 改为 `validate`，生产仍为 `none`；Flyway 禁用 clean，并启用现有库 `baseline-on-migrate`。
- 含破坏性删列逻辑的旧 `db/migration_drop_redundant_ids.sql` 已移入 `database/legacy/`，不会被 Flyway 自动执行。
- 使用 `/private/tmp` 随机目录的隔离 MySQL 9.6 完成双路径验证：空库执行 V1 后 69 表、历史类型 SQL；已有 68 表库只登记 baseline 后 69 表、历史类型 BASELINE；两次健康检查均 UP。
- 隔离后端和 MySQL 全部停止，约 210MB 临时数据目录已删除。正式 `CRS` 最终复核仍为 68 张业务表、0 张 Flyway 历史表，没有发生写入。
- 正式 baseline 因属于持久数据库结构变更被安全审批拦截，需用户明确批准。为避免普通重启误写正式库，本机 `.env.local` 当前为 `CRS_JPA_DDL_AUTO=validate` 且 `CRS_FLYWAY_ENABLED=false`。
- 全套基线继续通过：前端 36 tests、ESLint 265/48、后端 21 tests run / 0 failures / 0 errors / 1 skipped；Graphify 为 4,634 nodes / 5,217 edges / 413 communities。

## 2026-08-26：阶段 1.4 正式 Flyway baseline 完成

- 用户授权后，正式配置后端在 18087 启动，Flyway 为 `CRS` 新增 `flyway_schema_history` 并登记 V1 BASELINE；日志确认没有执行 V1 SQL。
- 正式库现为 68 张业务表 + 1 张历史表，V1 记录 type=BASELINE、success=1。订单等九个关键表行数与变更前完全一致。
- Hibernate `validate`、68 个 Repository 初始化和健康检查均通过，18087 临时后端已停止。
- 本机 `.env.local` 已正式启用 Flyway 并保持 Hibernate `validate`。阶段 1.4 完成，后续阶段按“实施—完整测试—记录—自动进入下一阶段”连续执行，仅在不可逆删除、需求取舍或外部权限缺失时询问用户。

## 2026-08-26：阶段 2 架构底座与取消政策 DTO 试点完成

- 用户要求以后每完成一个阶段并测试后自动进入下一阶段，直到计划内问题全部修复；PRD 继续只读。
- 新增统一 API 错误模型、全局异常边界、外部错误写入器和全请求 Trace Filter。认证、授权、租户错误均返回稳定错误码、兼容字段和 Trace；非法 Trace 会被替换，请求结束统一清理 MDC。
- 新增统一分页 `PageResponse` 与审计快照 `AuditContext`。
- 取消政策完成 DTO/Mapper 渐进试点：请求不再允许注入租户/主键/审计字段，响应 JSON 字段保持兼容，控制器不再直接依赖 Repository。
- 修复取消政策编辑遗漏类型、取消天数、截止时间和扣费类型的功能缺陷；删除引用统计增加 `groupId` 边界，消除跨租户误阻断。
- 全量基线通过：前端 36 tests、生产构建、ESLint 265/48 冻结线、Java 17 后端编译和全量测试均通过。
- 18088 运行态验收通过：Flyway/JPA validate、health UP、401 统一错误、Trace 传播/清洗以及登录后租户 1 取消政策只读查询均正常；独立实例已关闭。
- Graphify 更新为 4,707 nodes / 5,296 edges / 429 communities。下一步自动进入阶段 3 的订单状态与库存一致性治理。

## 2026-08-26：阶段 3 订单状态与库存一致性治理完成

- 只读体检正式库：260,063 条订单、7 条支付流水、1,129 条 PMS 库存；库存负数与配额超限均为 0。发现 3 条支付流水缺租户、3 条历史订单为 cancelled + paid。
- 新增纯领域 `ReservationStateMachine`；人工取消和状态变更改为租户级悲观锁。通用状态接口禁止绕过取消流程直接写 cancelled，取消原因/操作人在 Service 强校验。
- 已支付订单在没有真实退款执行器时采用 fail-closed，在释放库存和改状态前拒绝取消；历史 3 条财务状态未擅自改账。
- 酒店每日库存扣减先悲观锁定全部房型行，再计算酒店总量并原子扣减，关闭不同房型并发下单导致酒店级超卖的窗口。
- 支付流水创建补齐 tenantId；Flyway V2 已在正式库执行，回填 3 条空租户、将 tenant_id 设为 NOT NULL，并增加 `(reservation_id, transaction_id)` 唯一约束。
- V2 后订单和支付行数、支付金额均不变，Flyway success=1，18089 health UP；全量基线继续通过。
- Graphify 更新为 4,743 nodes / 5,355 edges / 433 communities。下一步自动进入阶段 4 的价格、集团与酒店模块治理。

## 2026-08-26：阶段 4 价格、集团与酒店边界治理完成

- PRD 继续只读。识别并修复价格计划、集团价格码和酒店创建接口直接保存请求实体导致的主键注入/merge 风险；客户端主键、租户和审计时间现在均由服务端覆盖。
- 新增 `RatePlanCommandService` 承接价格计划创建、更新、删除和状态命令；更新按租户加载现有实体后白名单复制业务字段，房价代码和类型保存后不可修改。
- 集团价格码创建/更新补齐同集团父级存在性、自引用和间接循环校验，房价代码和类型同样冻结；历史 3 条集团价格码不存在悬空或循环。
- 正式库价格计划 43 条、酒店 25 条，仅做只读检查，没有迁移或业务数据写入。
- 定向测试、全量基线和独立端口 18090 运行态验收全部通过；价格计划、集团价格码和酒店只读接口均返回 200。
- Graphify 更新为 4,783 nodes / 5,429 edges / 436 communities。下一步自动进入阶段 5 的渠道、PMS 与 Mock 数据治理。

## 2026-08-26：阶段 5 渠道、PMS 与 Mock 治理完成

- 渠道映射页面已连接真实渠道、酒店、房型、价格计划和三类映射 API；后端按租户主数据回填可信名称/归属，创建忽略客户端身份，更新采用白名单复制并拒绝重复映射。
- 酒店上下文及集团价格码、市场码、来源码、房价/房型分类的 API 失败路径不再回退到模拟数据；来源码删除和唯一性检查不再失败也成功。
- OTA/携程促销与活动在没有厂商接口时统一显示“尚未接入”，携程/飞猪详情改走真实通用渠道配置。
- 新增默认关闭的 PMS HMAC Webhook、5 分钟时间窗、主数据/数量校验和幂等收件箱；Flyway V3 正式成功，原 PMS 库存 1,129 条未变。
- 渠道与 PMS Adapter 端口已建立；缺少厂商地址、凭证和联调环境时保持安全关闭，不伪造出站成功。
- 全量基线通过，ESLint 降为 251/42，主包降为 4,302.14 KB；Graphify 为 4,861 nodes / 5,522 edges / 447 communities。下一步自动进入阶段 6 的路由懒加载和分包。

## 2026-08-26：阶段 6 路由懒加载与前端分包完成

- PRD 继续只读。仪表盘、订单、库存、酒店、集团房型/房价码、渠道、价格计划和报表等主要业务路由已改为 `React.lazy`，主布局提供统一异步加载态。
- Vite 将 React、Ant Design、图表、编辑器和通用依赖拆为可缓存 vendor 块；生产主入口从 4,302.14 kB 降为 220.91 kB，下降约 94.9%。
- Playwright 直接访问渠道映射与新增价格计划懒加载路由，未出现动态模块加载失败；回归中发现并修复多类接口包装响应被直接赋给数组状态导致的 `.map` 白屏。
- 全量基线通过：前端 3 suites / 36 tests、生产构建、ESLint 250/42、Java 17 后端编译与测试全部正常。
- Graphify 更新为 4,863 nodes / 5,524 edges / 447 communities。下一步自动进入阶段 7 的剩余 Mock、硬编码上下文、报表诊断与全链路收口。

## 2026-08-26：阶段 7 实施中，正式 Flyway V4 待明确批准

- PRD 保持只读。首页已停止注入随机趋势、虚构排行/预警与渠道数据，改为展示后端真实酒店、库存、趋势、流速、渠道营收和异常结果。
- 报表查询名称字典改为租户级读取，在线日期范围限制为最多 366 天、查询事务 10 秒；出租率/营收 GET 不再隐式删除重建汇总。预聚合写模型已拆到 `ReportSummaryService`，显式同步限制超级/集团管理员。
- 档案本地缓存假成功、集团房型硬编码集团 1 和角色页静态角色已移除；角色/菜单改走真实 API。集团设置新增后端租户级实体、DTO、Service、API 和 Flyway V4，前端不再以 localStorage 充当配置数据库。
- 数据导出已建立独立工作台；所有业务页面补齐路由懒加载。入口 JS 为 64.18 kB（gzip 16.84 kB），全量前后端基线通过，ESLint 降为 229 errors / 41 warnings。
- Playwright 回归首页、角色、集团设置、数据导出和档案 5 条路由通过，控制台 0 error。Graphify 更新为 4,917 nodes / 5,572 edges / 456 communities。
- 正式数据库 Flyway V4 仅新增 `group_settings` 表和 `tenant_id` 唯一索引，不修改/删除现有数据；执行被安全审批要求明确授权。得到用户批准前不执行迁移，也不重启正式服务。

## 2026-08-26：阶段 7 报表诊断与全链路收口完成

- 用户明确批准后，正式 CRS 数据库已执行 Flyway V4：新增 `group_settings` 表及租户唯一索引，历史记录为 `version=4/type=SQL/success=1`；没有删除或修改现有业务数据。
- 临时 18092 实例与正式 8080 实例均通过鉴权、租户上下文、集团设置和报表回归。集团设置表保持 0 行，报表汇总表查询前后均为 21,690 行，证明 GET 路径无隐式写入。
- 正式运行态响应：health 200/32ms、前端 200/3ms、集团设置 200/306ms、订单报表 200/313ms、出租率报表 200/153ms；超过 366 天查询按预期返回 400/60ms。
- `start-local.sh --restart` 已在 macOS Terminal 完整执行；正式后端 PID 36969 监听 8080，正式前端 PID 37312 监听 3001，PID 文件与监听进程一致。
- 阶段 7 全量质量结果：前端 36 tests、生产构建、Java 17 后端编译/测试全部通过；全仓 ESLint 229 errors / 41 warnings，受影响文件 0/0；Playwright 5 条核心路由通过且控制台 0 error。
- Graphify 在阶段代码完成后更新为 4,917 nodes / 5,572 edges / 456 communities。阶段 0–7 计划内修复已全部完成，PRD 全程未修改；缺少厂商凭据的 OTA/PMS 出站集成继续安全关闭，不伪造成功。

## 2026-08-26：阶段完成状态复核

- 用户询问是否已全部调整完成；复核项目记忆及运行态后确认，计划内阶段 0–7 均已完成。
- 正式后端 8080 health 与前端 3001 当前均返回 200。
- 无新增代码或 PRD 变更；OTA/PMS 厂商联调仍取决于外部地址、凭据和测试环境。

## 2026-08-26：需求级测试与全局 UI 精修方案待确认

- 用户要求依据需求清单执行页面、字段、功能级测试，并优化全系统 UI 体验；明确要求保持现有配色，其他体验可调整。
- 本轮已加载 Graphify、长期记忆和 PRD，只读扫描结果：SOW 含 33 个功能模块、142 条编号需求；前端有 84 条路由、60 个懒加载页面、100 个 JSX 页面。
- UI 技能基线判断为高密度企业运营台；其推荐新色板不采用。现有 `App.jsx`、主布局、全局样式和登录页的颜色将被冻结，只调整层级、字体、间距、圆角、阴影、表单、表格、反馈、响应式和无障碍。
- 当前 91 个页面含内联样式，共 1,493 处；全系统包含约 452 个 Form.Item 和 92 个 Table，粗糙感主要来自页面骨架、密度、对齐和状态反馈缺少统一规范，而非需要推倒重写。
- 拟建立“PRD 条目—路由—页面—字段—操作—API—预期—结果—证据”追踪矩阵，按全局基座、订单/库存、房型/价格/渠道、集团主数据、报表/系统设置五批渐进精修；每批完成后执行单元测试、构建、Lint、Playwright 桌面/平板/移动端回归和 Graphify 更新。
- PRD 继续只读。依项目 SOP，待用户对本轮 RFC 回复 `Proceed` 后开始代码和测试资产写入。

## 2026-08-26：需求页面、字段、功能回归与全局 UI 精修完成

- 按用户授权使用 `frontend-design`、`ui-ux-pro-max`、`webapp-testing` 与 Graphify 完成全系统体验收口；现有配色保持不变，PRD 未改动。
- 新增统一 `PageScaffold`、筛选面板、表格面板和全局体验样式；精修订单、库存、房态、预订控制、房型、价格计划、渠道映射、用户、集团设置与报表等高频页面，补齐字段标签、加载状态、响应式和辅助名称。
- 四档视口完成 35 核心路由审计：1440×1000、1024×900、375×812、812×375 均为 35/35；应用/动态加载/导航异常、控制台错误、5xx、页面横向溢出、无标签图标按钮均为 0。最终对照矩阵时补入曾遗漏的字典管理路由，并完成响应式精修与复测。
- 功能级回归 7 项全部通过：订单查询/重置/导出入口、房态批量表单、预订控制批量表单、房型新增字段、价格计划新增字段、基础价格真实查询、价格查询真实回显；临时价格夹具和日志均已清理。
- 清理永久模拟数据共 260,022 条订单（250,000 条 `RES_TST_%`、10,000 条 generator、22 条显式测试订单），数据库复核各测试条件及 `CODEX_UI_TEST_20260826` 价格/日志残留均为 0。该删除不可由项目直接恢复，必要时只能从历史备份恢复。
- 删除 21 个路由不可达或废弃的旧测试/重复/促销占位页面与设置工具文件，移除生产代码中的 Demo 模式、随机 UUID、API 失败 Mock 回退和静态模拟主数据；测试目录隔离 Mock 保留。
- 最终质量结果：前端 3 suites / 36 tests 全通过；后端 51 tests 中 50 通过、1 个预设跳过；Vite 4,972 modules 构建成功；本次 UI 涉及文件 ESLint 无 error。全项目剩余 109 个 error 均为历史未使用变量，其他类型 error 为 0。
- 正式本地服务保持运行：8080 health 为 `UP`，3001 返回 200。测试矩阵、最终基线和经验分别保存于 `.kiro/baseline/2026-08-26/` 与 `.kiro/memory/lessons/20260826_ui_requirement_testing_and_fixture_cleanup.md`。

## 2026-08-27：浏览器测试方式说明

- 用户询问最终测试是否通过页面模拟人工点击完成。
- 关键功能回归确实由 Playwright 驱动真实 Chromium 页面，按人工使用顺序执行登录、填写、点击、等待跳转、打开/关闭表单、查询和读取真实页面结果，不是仅调用接口后直接宣称页面通过。
- 35 路由四视口审计同样在真实浏览器中完成登录和逐路由访问，但其中多数页面属于只读 DOM、控制台、HTTP、溢出和可访问性检查，并未逐一执行全部新增、编辑、删除、发布等破坏性操作。
- 前端 Jest、后端 Maven 和数据库残留复核属于代码/API/数据层验证，不是页面点击；因此准确结论是“关键流程做了浏览器模拟人工点击，全量页面做了浏览器巡检，并以单元/后端/数据库验证补足”，而不是“142 条需求全部由人工点击逐项验收”。

## 2026-08-27：全场景页面破坏性回归与数据清理收口

- 用户要求将删除、停用、发布、分配、回收、订单取消等高风险操作也纳入页面模拟人工测试，并要求验证集团房价码下发酒店的跨模块回显与同步。
- 16 个 Playwright 脚本的页面/字段/功能生命周期全部通过；覆盖集团主数据、税费/包价/政策/设施/档案、集团房型、集团房价码、渠道映射、系统设置、库存/价格/房态/预订控制、订单、酒店/房型/房价计划/包价日价、报表和 UI 交互。
- 修复集团房价码编辑页的多个异步竞态：详情请求覆盖用户编辑、分配数据重复加载、渠道/酒店/发布记录未就绪即允许操作。现以请求代次和 tab loading 门禁保证最新数据写入表单。
- `ApiLoggingAspect` 改为注入 Spring Boot 已配置的 `ObjectMapper`，解决 `LocalDate` 无法序列化导致系统 Trace 日志反复保存失败的问题；新鲜日志中对应序列化异常与 HTTP 500 均为 0。
- 生产代码移除路由不可达/重复页面、两个旧酒店测试脚本、一个旧房价计划脚本、七个 `.tmp` Java 副本、Python 缓存及未使用页面/变量；生产路径不再含 API 失败 Mock 回退。
- 最终质量：UI 35/35 路由，应用/console/5xx/溢出/无标签按钮均为 0；前端 36/36，后端 52 tests 中 0 failures / 0 errors / 1 跳过，生产构建通过，ESLint 0 errors / 55 warnings。
- 全库独立审计发现并删除 15 条早期 `CODEX_GRC_*` 和 5 条 `CODEX_GRT_*` 孤立夹具。根因是两个清理脚本按 `tenant_id` 过滤，但集团主表实际使用 `group_id`，使清理计数假为 0。已改为按测试全局唯一代码清理，重跑两条完整链路后全库复扫为 0。
- `graphify update .` 已完成：469 个文件、5,135 nodes、6,130 edges、451 communities，抽取率 100%。
- 最终通过 macOS Terminal 执行 `./start-local.sh --restart` 使服务持续运行；后端 PID 90756，8080 health 为 `UP`；前端 PID 91167，3001 返回 HTTP 200。
- 本阶段未修改 PRD；测试结果只同步到 baseline、lessons 和本记忆。
