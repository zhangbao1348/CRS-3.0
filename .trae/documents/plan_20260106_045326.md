1. **修改控制类型选择下拉框**：将当前的"排程"和"其他"枚举选项改为渠道列表
2. **使用渠道数据填充下拉框**：将mockChannels数组中的渠道名称作为下拉框的选项
3. **更新状态管理**：将selectedControlType状态改为selectedChannel，对应渠道选择
4. **更新相关逻辑**：确保所有使用selectedControlType的地方都更新为selectedChannel
5. **测试修复后的页面**：确保下拉框显示正确的渠道选项，并且功能正常

修复后，控制类型选择下拉框将显示与渠道选择对应的选项，方便用户进行渠道筛选操作。