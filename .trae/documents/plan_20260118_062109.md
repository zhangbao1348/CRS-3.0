### 调整所有二级菜单位置的完整解决方案

**问题描述**：当前部分二级菜单（如房型管理的子菜单）没有正确向右移动，需要修改样式确保所有二级菜单都能受到影响。

**修改方案**：

1. **重新分析Ant Design 5.x菜单组件结构**：
   - 查看实际渲染的HTML结构和类名
   - 确定正确的选择器

2. **修改CSS样式**：
   - 使用更具体的选择器覆盖Ant Design默认样式
   - 确保所有类型的二级菜单都能受到影响
   - 调整样式优先级

3. **测试修改效果**：
   - 确保所有二级菜单都向右移动
   - 确保没有破坏其他功能

**修改文件**：

* `src/styles/index.css`

**修改步骤**：

1. 移除之前添加的可能无效的样式
2. 添加针对Ant Design 5.x的正确样式
3. 使用更具体的选择器确保样式优先级
4. 针对内联菜单和弹出菜单分别调整样式

**预期效果**：

* 所有二级菜单（包括房型管理的子菜单）都向右移动
* 保持菜单的原有功能
* 不影响其他页面元素

**技术要点**：

* 理解Ant Design 5.x菜单组件的结构
* 使用正确的CSS选择器
* 调整样式优先级
* 确保所有类型的菜单都能受到影响

**风险评估**：

* 低风险，仅修改样式，不影响核心功能
* 可能需要多次调整才能找到正确的选择器

**测试建议**：

* 检查所有带有子菜单的菜单项
* 验证菜单的展开和收起功能
* 验证菜单的点击功能