1. **修复Tabs.TabPane的使用方式**：在Ant Design 5.x版本中，`Tabs.TabPane`已不再推荐使用，应直接使用`Tabs`的`items`属性

2. **修复Form.Item的使用方式**：在Ant Design 5.x版本中，`Form.Item`应直接使用，而不是通过`const { Item } = Form`获取

3. **修复表格列配置中的dataIndex**：将数组形式的`dataIndex: ['roomTypeCode', 'roomTypeName']`改为对象形式

4. **检查并更新其他可能的API变化**：确保所有组件使用的都是Ant Design 5.x版本的正确API

5. **测试修复后的页面**：确保页面能够正常渲染和交互

修复后，房控日历页面应该能够正常运行，不再报错。