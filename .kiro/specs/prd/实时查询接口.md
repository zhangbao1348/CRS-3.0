# CRS 酒店预订实时查询接口 — PRD

> **版本**：v1.0  
> **更新日期**：2026-04-30  
> **使用方**：小程序端  
> **状态**：[ ] 未实现

---

## 一、概述

本文档定义了 CRS 中央预订系统面向小程序端的实时查询与预订接口。所有接口通过渠道秘钥（API Key + Secret）进行身份认证，数据按租户隔离。

### 接口清单

| 序号 | 接口名称 | 方法 | 路径 | 状态 |
|------|----------|------|------|------|
| 1 | 查询酒店列表 | GET | `/api/open/hotels` | ✅ 已实现 |
| 2 | 查询酒店详情与价格 | GET | `/api/open/hotels/{hotelCode}` | ✅ 已实现 |
| 3 | 可订检查 | POST | `/api/open/availability/check` | ✅ 已实现 |
| 4 | 创建订单 | POST | `/api/open/reservations` | [ ] 未实现 |
| 5 | 支付订单 | POST | `/api/open/reservations/{reservationCode}/pay` | [ ] 未实现 |

### 公共约定

- **Base URL**：`https://{domain}/api/open`
- **认证方式**：所有请求必须在 Header 中携带 `X-Api-Key` 和 `X-Api-Secret`，对应 `tenant_channels` 表的 `access_key` / `access_secret`
- **数据格式**：JSON（`Content-Type: application/json`）
- **日期格式**：`yyyy-MM-dd`（如 `2026-05-01`）
- **金额单位**：人民币元，保留两位小数
- **字符编码**：UTF-8

### 公共响应结构

```json
{
  "code": 200,
  "message": "success",
  "data": { ... },
  "timestamp": "2026-05-01T10:00:00+08:00"
}
```

### 公共错误码

| 错误码 | 含义 |
|--------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 认证失败（Key/Secret 无效或渠道未连接） |
| 403 | 权限不足（渠道未被授权访问该酒店） |
| 404 | 资源不存在 |
| 409 | 库存不足或不可预订 |
| 500 | 服务器内部错误 |

---

## 二、接口详细设计

---

### 接口 1：查询酒店列表

获取渠道可见的酒店列表，包含静态信息和起价。

- **方法**：`GET`
- **路径**：`/api/open/hotels`
- **状态**：[ ] 未实现

#### 请求参数（Query）

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| cityId | String | 否 | 城市筛选，对应 `hotels.city` |
| checkInDate | String | 否 | 入住日期（yyyy-MM-dd），用于计算起价和可用性 |
| checkOutDate | String | 否 | 离店日期（yyyy-MM-dd），与 checkInDate 配对使用 |
| keyword | String | 否 | 搜索关键词，模糊匹配酒店中/英文名称 |
| page | Integer | 否 | 页码，默认 1 |
| pageSize | Integer | 否 | 每页数量，默认 20，最大 50 |

#### 业务规则

1. 仅返回 `status = active` 的酒店
2. 仅返回已通过 `channel_hotel_mappings` 授权给当前渠道的酒店
3. 若传入 `checkInDate` 和 `checkOutDate`，则计算该日期范围内的最低起价（从 `hotel_prices` 表取含税价最低值）
4. 若未传入日期，起价字段返回 `null`
5. 起价计算需排除已关房（`room_status` 为 closed）的房型

#### 响应数据

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "total": 15,
    "page": 1,
    "pageSize": 20,
    "list": [
      {
        "hotelCode": "SHHX001",
        "chineseName": "上海虹桥酒店",
        "englishName": "Shanghai Hongqiao Hotel",
        "city": "上海",
        "province": "上海",
        "address": "上海市长宁区虹桥路88号",
        "phone": "021-12345678",
        "starRating": "5",
        "latitude": 31.1956,
        "longitude": 121.3369,
        "totalRooms": 200,
        "introduction": "酒店简介...",
        "startingPrice": 688.00,
        "currency": "CNY",
        "images": [
          {
            "imageType": "main",
            "imagePath": "https://cdn.example.com/hotels/SHHX001/main.jpg",
            "imageName": "酒店外观",
            "description": "酒店外观照片",
            "sortOrder": 1
          },
          {
            "imageType": "lobby",
            "imagePath": "https://cdn.example.com/hotels/SHHX001/lobby.jpg",
            "imageName": "酒店大堂",
            "description": "大堂全景",
            "sortOrder": 2
          },
          {
            "imageType": "restaurant",
            "imagePath": "https://cdn.example.com/hotels/SHHX001/restaurant.jpg",
            "imageName": "餐厅",
            "description": "自助餐厅",
            "sortOrder": 3
          }
        ],
        "facilities": [
          {
            "facilityType": "general",
            "facilityCode": "wifi",
            "facilityName": "免费WiFi"
          },
          {
            "facilityType": "recreation",
            "facilityCode": "pool",
            "facilityName": "室内游泳池"
          }
        ],
        "roomTypes": [
          {
            "roomTypeCode": "STDK",
            "roomTypeName": "标准大床房",
            "englishName": "Standard King Room",
            "description": "35平米，高层景观",
            "maxOccupancy": 2,
            "maxAdults": 2,
            "maxChildren": 1,
            "area": 35,
            "floor": "8-15",
            "windowType": "有窗",
            "bedType": "大床 1.8m",
            "roomQuantity": 50,
            "sortOrder": 1,
            "image": "https://cdn.example.com/rooms/STDK/main.jpg",
            "images": [
              {
                "imagePath": "https://cdn.example.com/rooms/STDK/main.jpg",
                "imageName": "房间全景",
                "sortOrder": 1
              },
              {
                "imagePath": "https://cdn.example.com/rooms/STDK/bath.jpg",
                "imageName": "卫浴",
                "sortOrder": 2
              }
            ],
            "facilities": [
              {
                "facilityType": "bathroom",
                "facilityCode": "private_bath",
                "facilityName": "独立卫浴"
              },
              {
                "facilityType": "room",
                "facilityCode": "minibar",
                "facilityName": "迷你吧"
              }
            ]
          }
        ]
      }
    ]
  }
}
```

#### 响应字段说明

**酒店基础字段**

| 字段 | 类型 | 说明 | 数据来源 |
|------|------|------|----------|
| hotelCode | String | 酒店唯一编码 | `hotels.hotel_code` |
| chineseName | String | 酒店中文名称 | `hotels.chinese_name` |
| englishName | String | 酒店英文名称 | `hotels.english_name` |
| city | String | 所在城市 | `hotels.city` |
| province | String | 所在省份 | `hotels.province` |
| address | String | 详细地址 | `hotels.address` |
| phone | String | 联系电话 | `hotels.phone` |
| starRating | String | 星级评定 | `hotels.star_rating` |
| latitude | Double | 纬度坐标 | `hotels.latitude` |
| longitude | Double | 经度坐标 | `hotels.longitude` |
| totalRooms | Integer | 酒店总房间数 | `hotels.total_rooms` |
| introduction | String | 酒店简介描述 | `hotels.introduction` |
| startingPrice | Decimal | 起价（含税），未传入住日期时为 null | `hotel_prices.price_with_tax` 日期范围内最低值 |
| currency | String | 币种，固定返回 "CNY" | 常量 |

**酒店图片字段（images[]）— 返回所有种类的图片**

| 字段 | 类型 | 说明 | 数据来源 |
|------|------|------|----------|
| imageType | String | 图片类型（如 main/lobby/restaurant/pool/room/meeting 等） | `hotel_images.image_type` |
| imagePath | String | 图片完整 URL | `hotel_images.image_path` |
| imageName | String | 图片显示名称 | `hotel_images.image_name` |
| description | String | 图片描述说明 | `hotel_images.description` |
| sortOrder | Integer | 排序序号，数值越小越靠前 | `hotel_images.sort_order` |

**酒店设施字段（facilities[]）— 返回设施类型、代码和名称**

| 字段 | 类型 | 说明 | 数据来源 |
|------|------|------|----------|
| facilityType | String | 设施分类（如 general/recreation/business/dining 等） | `hotel_facilities.facility_type` |
| facilityCode | String | 设施唯一代码 | `hotel_facilities.facility_code` |
| facilityName | String | 设施中文名称 | `hotel_facilities.facility_name` |

**房型静态信息字段（roomTypes[]）**

| 字段 | 类型 | 说明 | 数据来源 |
|------|------|------|----------|
| roomTypeCode | String | 房型编码 | `hotel_room_types.room_type_code` |
| roomTypeName | String | 房型中文名称 | `hotel_room_types.room_type_name` |
| englishName | String | 房型英文名称 | `hotel_room_types.english_name` |
| description | String | 房型描述 | `hotel_room_types.description` |
| maxOccupancy | Integer | 最大入住人数 | `hotel_room_types.max_occupancy` |
| maxAdults | Integer | 最大成人数，= max_occupancy - max_children | 计算值 |
| maxChildren | Integer | 最大儿童数 | `hotel_room_types.max_children` |
| area | Integer | 房间面积（平方米） | `hotel_room_types.area` |
| floor | String | 所在楼层范围 | `hotel_room_types.floor` |
| windowType | String | 窗型（有窗/无窗/内窗） | `hotel_room_types.window_type` |
| bedType | String | 床型描述 | `hotel_room_types.bed_type` |
| roomQuantity | Integer | 该房型总房间数 | `hotel_room_types.total_rooms` |
| sortOrder | Integer | 排序序号 | `hotel_room_types.sort_order` |
| image | String | 房型主图 URL | `hotel_room_types.image`（第一张） |
| images[] | Array | 房型所有图片列表（imagePath / imageName / sortOrder） | `hotel_room_types.image`（JSON 数组解析） |
| facilities[] | Array | 房型设施列表（facilityType / facilityCode / facilityName） | `room_type_facilities`（`available = true`） |

---

### 接口 2：查询酒店详情与价格

获取单个酒店的完整信息，包含所有可售房型及每日价格。

- **方法**：`GET`
- **路径**：`/api/open/hotels/{hotelCode}`
- **状态**：[ ] 未实现

#### 路径参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| hotelCode | String | 是 | 酒店编码 |

#### 请求参数（Query）

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| checkInDate | String | 是 | 入住日期（yyyy-MM-dd） |
| checkOutDate | String | 是 | 离店日期（yyyy-MM-dd） |
| adultCount | Integer | 否 | 成人数，默认 2 |
| childCount | Integer | 否 | 儿童数，默认 0 |
| roomCount | Integer | 否 | 房间数，默认 1 |
| memberNo | String | 否 | 会员编号，用于匹配会员专属价格计划 |
| memberLevel | String | 否 | 会员等级（如 silver/gold/platinum/diamond），用于筛选对应等级可见的价格计划 |

#### 业务规则

1. 校验渠道是否有权访问该酒店（`channel_hotel_mappings`）
2. 仅返回 `status = active` 的房型（`hotel_room_types`）
3. 仅返回已关联到当前渠道且处于可售状态的价格计划（`rate_plans`）
4. 每日价格从 `hotel_prices` 表获取，日期范围为 `[checkInDate, checkOutDate)`
5. 库存从 `inventory` 表获取，按当前渠道的 `channel_code` 过滤，取日期范围内的最小可用值（`MIN(available_rooms)`）
6. 房态检查：若该房型在 `room_status` 中 `is_open = false`，标记为不可订（无记录时默认开房）
7. 预订控制：从 `booking_controls` 获取提前预订天数、最少/最多入住天数等规则
8. 包价信息从 `rate_plans.packages` JSON 字段解析，再关联 `packages` 表获取包价详情
9. 若传入 `memberNo` 或 `memberLevel`，额外返回会员专属价格计划（`rate_plans` 中 `personal_membership` JSON 字段匹配对应等级的记录）
10. 未传会员信息时，仅返回 `personal_membership` 为空的价格计划（即非会员专属价格计划）

#### 响应数据

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "hotel": {
      "hotelCode": "SHHX001",
      "chineseName": "上海虹桥酒店",
      "englishName": "Shanghai Hongqiao Hotel",
      "city": "上海",
      "province": "上海",
      "address": "上海市长宁区虹桥路88号",
      "phone": "021-12345678",
      "email": "info@shhx.com",
      "starRating": "5",
      "latitude": 31.1956,
      "longitude": 121.3369,
      "totalRooms": 200,
      "introduction": "酒店简介...",
      "images": [
        {
          "imageType": "main",
          "imagePath": "https://cdn.example.com/hotels/SHHX001/main.jpg",
          "imageName": "酒店外观",
          "description": "酒店外观照片",
          "sortOrder": 1
        }
      ],
      "facilities": [
        {
          "facilityType": "general",
          "facilityName": "免费WiFi",
          "facilityCode": "wifi"
        }
      ]
    },
    "roomTypes": [
      {
        "roomTypeCode": "STDK",
        "roomTypeName": "标准大床房",
        "englishName": "Standard King Room",
        "description": "房型描述...",
        "maxOccupancy": 2,
        "maxAdults": 2,
        "maxChildren": 1,
        "area": 35,
        "floor": "8-15",
        "windowType": "有窗",
        "bedType": "大床 1.8m",
        "image": "https://cdn.example.com/rooms/STDK.jpg",
        "facilities": [
          {
            "facilityType": "bathroom",
            "facilityName": "独立卫浴",
            "facilityCode": "private_bath"
          }
        ],
        "ratePlans": [
          {
            "ratePlanCode": "BAR",
            "ratePlanName": "最优房价",
            "rateType": "public",
            "rateCategory": "BAR",
            "memberLevelCode": null,
            "description": "含早餐",
            "availableRooms": 5,
            "totalPrice": 1376.00,
            "averagePrice": 688.00,
            "currency": "CNY",
            "dailyPrices": [
              { "date": "2026-05-01", "priceWithTax": 688.00 },
              { "date": "2026-05-02", "priceWithTax": 688.00 }
            ],
            "packages": [
              {
                "packageCode": "BF",
                "packageName": "中西自助早餐",
                "type": "breakfast",
                "typeName": "早餐",
                "frequency": "daily",
                "quantity": 2
              }
            ],
            "cancellationPolicy": {
              "type": "limited",
              "description": "入住前1天18:00前免费取消，之后扣首晚房费",
              "freeCancelBeforeDays": 1,
              "freeCancelBeforeTime": "18:00",
              "penaltyType": "first_night"
            },
            "guaranteePolicy": {
              "type": "credit_card",
              "subType": "overtime_guarantee",
              "amount": "first_night",
              "latestArrivalTime": "18:00",
              "description": "18:00后到店需信用卡担保首晚房费"
            },
            "bookingRules": {
              "advanceBookingMin": 0,
              "advanceBookingMax": 90,
              "minimumStay": 1,
              "maximumStay": 30
            }
          }
        ]
      }
    ]
  }
}
```

#### 响应字段详细说明

**酒店信息（hotel）**

| 字段路径 | 类型 | 说明 | 数据来源 |
|----------|------|------|----------|
| `hotel.hotelCode` | String | 酒店唯一编码 | `hotels.hotel_code` |
| `hotel.chineseName` | String | 酒店中文名称 | `hotels.chinese_name` |
| `hotel.englishName` | String | 酒店英文名称 | `hotels.english_name` |
| `hotel.city` | String | 所在城市 | `hotels.city` |
| `hotel.province` | String | 所在省份 | `hotels.province` |
| `hotel.address` | String | 详细地址 | `hotels.address` |
| `hotel.phone` | String | 联系电话 | `hotels.phone` |
| `hotel.email` | String | 联系邮箱 | `hotels.email` |
| `hotel.starRating` | String | 星级评定 | `hotels.star_rating` |
| `hotel.latitude` | Double | 纬度 | `hotels.latitude` |
| `hotel.longitude` | Double | 经度 | `hotels.longitude` |
| `hotel.totalRooms` | Integer | 总房间数 | `hotels.total_rooms` |
| `hotel.introduction` | String | 酒店简介 | `hotels.introduction` |
| `hotel.images[]` | Array | 酒店所有图片（同接口1图片字段） | `hotel_images` |
| `hotel.facilities[]` | Array | 酒店设施列表（同接口1设施字段） | `hotel_facilities` |

**房型信息（roomTypes[]）**

| 字段路径 | 类型 | 说明 | 数据来源 |
|----------|------|------|----------|
| `roomTypes[].roomTypeCode` | String | 房型编码 | `hotel_room_types.room_type_code` |
| `roomTypes[].roomTypeName` | String | 房型中文名称 | `hotel_room_types.room_type_name` |
| `roomTypes[].englishName` | String | 房型英文名称 | `hotel_room_types.english_name` |
| `roomTypes[].description` | String | 房型描述 | `hotel_room_types.description` |
| `roomTypes[].maxOccupancy` | Integer | 最大入住人数 | `hotel_room_types.max_occupancy` |
| `roomTypes[].maxAdults` | Integer | 最大成人数，= max_occupancy - max_children | 计算值 |
| `roomTypes[].maxChildren` | Integer | 最大儿童数 | `hotel_room_types.max_children` |
| `roomTypes[].area` | Integer | 房间面积（㎡） | `hotel_room_types.area` |
| `roomTypes[].floor` | String | 楼层范围 | `hotel_room_types.floor` |
| `roomTypes[].windowType` | String | 窗型 | `hotel_room_types.window_type` |
| `roomTypes[].bedType` | String | 床型 | `hotel_room_types.bed_type` |
| `roomTypes[].image` | String | 房型主图 URL | `hotel_room_types.image` |
| `roomTypes[].facilities[]` | Array | 房型设施（facilityType/facilityCode/facilityName） | `room_type_facilities`（`available = true`） |

**价格计划信息（roomTypes[].ratePlans[]）**

| 字段路径 | 类型 | 说明 | 数据来源 |
|----------|------|------|----------|
| `ratePlans[].ratePlanCode` | String | 价格计划编码 | `rate_plans.rate_code` |
| `ratePlans[].ratePlanName` | String | 价格计划名称 | `rate_plans.rate_name` |
| `ratePlans[].rateType` | String | 价格计划层级类型（basic/level1/level2/derivative） | `rate_plans.rate_type` |
| `ratePlans[].rateCategory` | String | 房价大类编码 | `rate_plans.rate_category` |
| `ratePlans[].memberLevelCode` | String | 会员等级代码，非会员价时为 null | `rate_plans.personal_membership` JSON 解析 |
| `ratePlans[].description` | String | 价格计划描述 | `rate_plans.description` |
| `ratePlans[].availableRooms` | Integer | 可用房间数（日期范围内每天最小值，按当前渠道的 channel_code 查询） | `inventory.available_rooms` MIN，WHERE channel_code = 当前渠道 |
| `ratePlans[].totalPrice` | Decimal | 总价（所有入住日含税价之和 × roomCount） | `hotel_prices.price_with_tax` SUM |
| `ratePlans[].averagePrice` | Decimal | 均价（totalPrice / nights） | 计算值 |
| `ratePlans[].currency` | String | 币种 | 常量 "CNY" |

**每日价格（ratePlans[].dailyPrices[]）**

| 字段路径 | 类型 | 说明 | 数据来源 |
|----------|------|------|----------|
| `dailyPrices[].date` | String | 日期（yyyy-MM-dd） | `hotel_prices.price_date` |
| `dailyPrices[].priceWithTax` | Decimal | 该日含税单价 | `hotel_prices.price_with_tax` |

**包价信息（ratePlans[].packages[]）**

| 字段路径 | 类型 | 说明 | 数据来源 |
|----------|------|------|----------|
| `packages[].packageCode` | String | 包价编码 | `packages.code` |
| `packages[].packageName` | String | 包价名称 | `packages.name` |
| `packages[].type` | String | 包价类型代码（见下方包价类型枚举） | `packages.type` |
| `packages[].typeName` | String | 包价类型中文名称 | 根据 type 映射 |
| `packages[].frequency` | String | 发放频率（daily/once/per_stay） | `packages.frequency` |
| `packages[].quantity` | Integer | 份数 | `packages.fixed_quantity` |

**包价类型枚举（packages[].type）**

| type 值 | typeName | 说明 |
|---------|----------|------|
| breakfast | 早餐 | 早餐类包价，如中西自助早餐 |
| lunch | 午餐 | 午餐类包价 |
| dinner | 晚餐 | 晚餐类包价 |
| afternoon_tea | 下午茶 | 下午茶包价 |
| minibar | 迷你吧 | 迷你吧消费额度 |
| spa | SPA/水疗 | SPA 或水疗体验 |
| parking | 停车 | 免费停车服务 |
| airport_transfer | 接送机 | 机场接送服务 |
| laundry | 洗衣 | 洗衣服务 |
| gym | 健身 | 健身房使用 |
| pool | 泳池 | 泳池使用 |
| wifi | 上网 | WiFi 或商务中心上网 |
| voucher | 代金券 | 餐饮或消费代金券 |
| gift | 礼品 | 欢迎礼品 |
| upgrade | 升级 | 房型升级 |
| late_checkout | 延迟退房 | 延迟退房服务 |
| other | 其他 | 其他类型包价 |

**取消政策（ratePlans[].cancellationPolicy）**

| 字段路径 | 类型 | 说明 | 数据来源 |
|----------|------|------|----------|
| `cancellationPolicy.type` | String | 取消类型（free/limited/non_refundable） | `cancellation_policies.type` |
| `cancellationPolicy.description` | String | 取消政策文案 | 拼接生成 |
| `cancellationPolicy.freeCancelBeforeDays` | Integer | 免费取消截止天数（入住前N天） | `cancellation_policies.cancellation_days` |
| `cancellationPolicy.freeCancelBeforeTime` | String | 免费取消截止时间（HH:mm） | `cancellation_policies.cancellation_time` |
| `cancellationPolicy.penaltyType` | String | 罚金类型（first_night/full_amount） | `cancellation_policies.cancellation_fee_type` |

**担保政策（ratePlans[].guaranteePolicy）**

| 字段路径 | 类型 | 说明 | 数据来源 |
|----------|------|------|----------|
| `guaranteePolicy.type` | String | 担保类型（none/credit_card/prepay） | `guarantee_policies.type` |
| `guaranteePolicy.subType` | String | 担保子类型（always/overtime_guarantee） | `guarantee_policies.guarantee_sub_type` |
| `guaranteePolicy.amount` | String | 担保金额类型（first_night/full_amount） | `guarantee_policies.guarantee_amount` |
| `guaranteePolicy.latestArrivalTime` | String | 最晚到店时间（HH:mm），超时担保时有值 | `guarantee_policies.latest_arrival_time` |
| `guaranteePolicy.description` | String | 担保政策文案 | 拼接生成 |

**预订规则（ratePlans[].bookingRules）**

| 字段路径 | 类型 | 说明 | 数据来源 |
|----------|------|------|----------|
| `bookingRules.advanceBookingMin` | Integer | 最少提前预订天数 | 优先取 `booking_controls.advance_booking_days`（日期级），无记录时取 `rate_plans.advance_booking_min` |
| `bookingRules.advanceBookingMax` | Integer | 最多提前预订天数 | `rate_plans.advance_booking_max` |
| `bookingRules.minimumStay` | Integer | 最少入住天数 | 优先取 `booking_controls.min_stay`（日期级），无记录时取 `rate_plans.minimum_stay_min` |
| `bookingRules.maximumStay` | Integer | 最多入住天数 | 优先取 `booking_controls.max_stay`（日期级），无记录时取 `rate_plans.minimum_stay_max` |

---

### 接口 3：可订检查

下单前的实时可订检查，验证库存、房态、预订规则和价格。

- **方法**：`POST`
- **路径**：`/api/open/availability/check`
- **状态**：[ ] 未实现

#### 请求体

```json
{
  "hotelCode": "SHHX001",
  "roomTypeCode": "STDK",
  "ratePlanCode": "BAR",
  "checkInDate": "2026-05-01",
  "checkOutDate": "2026-05-03",
  "roomCount": 1,
  "adultCount": 2,
  "childCount": 0,
  "memberNo": "M100001",
  "memberLevel": "gold"
}
```

#### 请求字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| hotelCode | String | 是 | 酒店编码 |
| roomTypeCode | String | 是 | 房型编码 |
| ratePlanCode | String | 是 | 价格计划编码 |
| checkInDate | String | 是 | 入住日期 |
| checkOutDate | String | 是 | 离店日期 |
| roomCount | Integer | 是 | 房间数 |
| adultCount | Integer | 是 | 成人数 |
| childCount | Integer | 否 | 儿童数，默认 0 |
| memberNo | String | 否 | 会员编号，预订会员价时必填 |
| memberLevel | String | 否 | 会员等级，预订会员价时必填 |

#### 业务规则（按优先级顺序校验）

1. **渠道权限**：校验渠道是否有权访问该酒店
2. **酒店/房型/价格计划有效性**：校验三者是否存在且 `status = active`
3. **房型适用性**：校验该价格计划的 `applicable_room_types` 是否包含目标房型
4. **会员等级校验**：若价格计划为会员价（`rate_type = 'member'`），校验 `memberLevel` 是否满足该价格计划的 `personal_membership` 要求
5. **房态检查**：校验入住期间每一天的 `room_status.is_open = true`（无记录时默认开房）
6. **库存检查**：校验入住期间每一天的 `inventory.available_rooms >= roomCount`（按当前渠道 channel_code 过滤）
7. **入住人数**：校验 `adultCount <= hotel_room_types.max_adults`，`adultCount + childCount <= max_occupancy`
8. **提前预订天数**：校验 `checkInDate - today >= booking_controls.advance_booking_days`
9. **入住天数限制**：校验 `nights >= min_stay && nights <= max_stay`
10. **价格完整性**：校验入住期间每一天在 `hotel_prices` 中都有对应价格记录

#### 响应数据

```json
{
  "code": 200,
  "message": "success",
  "data": {
    "available": true,
    "hotelCode": "SHHX001",
    "roomTypeCode": "STDK",
    "ratePlanCode": "BAR",
    "checkInDate": "2026-05-01",
    "checkOutDate": "2026-05-03",
    "nights": 2,
    "roomCount": 1,
    "availableRooms": 5,
    "dailyPrices": [
      { "date": "2026-05-01", "priceWithTax": 688.00 },
      { "date": "2026-05-02", "priceWithTax": 688.00 }
    ],
    "totalPrice": 1376.00,
    "currency": "CNY",
    "packages": [
      {
        "packageCode": "BF",
        "packageName": "中西自助早餐",
        "type": "breakfast",
        "typeName": "早餐",
        "frequency": "daily",
        "quantity": 2
      }
    ],
    "cancellationPolicy": {
      "type": "limited",
      "description": "入住前1天18:00前免费取消，之后扣首晚房费"
    },
    "guaranteePolicy": {
      "type": "credit_card",
      "description": "18:00后到店需信用卡担保首晚房费"
    }
  }
}
```

#### 不可订时的响应

```json
{
  "code": 409,
  "message": "库存不足",
  "data": {
    "available": false,
    "reason": "INSUFFICIENT_INVENTORY",
    "reasonDescription": "2026-05-02 可用库存为 0，不满足所需房间数 1",
    "unavailableDates": ["2026-05-02"]
  }
}
```

#### 不可订原因枚举

| reason 值 | 说明 |
|-----------|------|
| HOTEL_INACTIVE | 酒店已停用 |
| ROOM_TYPE_INACTIVE | 房型已停用 |
| RATE_PLAN_INACTIVE | 价格计划已停用 |
| ROOM_TYPE_NOT_APPLICABLE | 价格计划不适用该房型 |
| ROOM_CLOSED | 房态已关闭 |
| INSUFFICIENT_INVENTORY | 库存不足 |
| EXCEED_MAX_OCCUPANCY | 超出最大入住人数 |
| ADVANCE_BOOKING_VIOLATION | 不满足提前预订天数要求 |
| STAY_DURATION_VIOLATION | 不满足入住天数限制 |
| PRICE_NOT_SET | 部分日期未设置价格 |
| MEMBER_LEVEL_MISMATCH | 会员等级不满足该价格计划要求 |
| MEMBER_INFO_REQUIRED | 会员价需提供会员编号和等级 |

---

### 接口 4：创建订单

提交预订订单，创建前会自动执行可订检查。

- **方法**：`POST`
- **路径**：`/api/open/reservations`
- **状态**：[ ] 未实现

#### 请求体

```json
{
  "hotelCode": "SHHX001",
  "roomTypeCode": "STDK",
  "ratePlanCode": "BAR",
  "checkInDate": "2026-05-01",
  "checkOutDate": "2026-05-03",
  "roomCount": 1,
  "adultCount": 2,
  "childCount": 0,
  "contact": {
    "name": "张三",
    "phone": "13800138000",
    "email": "zhangsan@example.com"
  },
  "guests": [
    {
      "name": "张三",
      "phone": "13800138000",
      "idType": "id_card",
      "idNumber": "310***********1234"
    },
    {
      "name": "李四",
      "phone": "13900139000",
      "idType": "id_card",
      "idNumber": "310***********5678"
    }
  ],
  "memberNo": "M100001",
  "memberLevel": "gold",
  "guaranteeInfo": {
    "type": "credit_card",
    "creditCardInfo": "加密的信用卡信息"
  },
  "specialRequest": "高层无烟房",
  "notes": "预计下午3点到店"
}
```

#### 请求字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| hotelCode | String | 是 | 酒店编码 |
| roomTypeCode | String | 是 | 房型编码 |
| ratePlanCode | String | 是 | 价格计划编码 |
| checkInDate | String | 是 | 入住日期 |
| checkOutDate | String | 是 | 离店日期 |
| roomCount | Integer | 是 | 房间数 |
| adultCount | Integer | 是 | 成人数 |
| childCount | Integer | 否 | 儿童数，默认 0 |
| contact.name | String | 是 | 联系人姓名 |
| contact.phone | String | 是 | 联系人手机号 |
| contact.email | String | 否 | 联系人邮箱 |
| guests[] | Array | 是 | 入住人列表，至少 1 人 |
| guests[].name | String | 是 | 入住人姓名 |
| guests[].phone | String | 否 | 入住人手机号 |
| guests[].idType | String | 否 | 证件类型（id_card/passport/other） |
| guests[].idNumber | String | 否 | 证件号码 |
| memberNo | String | 否 | 会员编号，预订会员价时必填 |
| memberLevel | String | 否 | 会员等级，预订会员价时必填 |
| guaranteeInfo.type | String | 条件必填 | 担保类型，当价格计划有担保要求时必填 |
| guaranteeInfo.creditCardInfo | String | 条件必填 | 信用卡信息（加密），担保类型为 credit_card 时必填 |
| specialRequest | String | 否 | 特殊需求 |
| notes | String | 否 | 备注 |

#### 业务规则

1. 创建前自动执行「接口 3：可订检查」的全部校验
2. 校验通过后，生成唯一预订号（`reservation_code`），格式：`CRS + 年月日 + 6位序列号`（如 `CRS20260501000001`）
3. 写入 `reservation` 表，初始状态：`status = active`，`reservation_status = confirmed`，`payment_status = unpaid`
4. 扣减 `inventory.available_rooms`（日期范围内每天减去 roomCount）
5. `channel_id` 通过认证的 API Key 关联的渠道记录获取
6. `created_by` 记录为渠道编码

#### 响应数据

```json
{
  "code": 200,
  "message": "预订成功",
  "data": {
    "reservationCode": "CRS20260501000001",
    "hotelCode": "SHHX001",
    "hotelName": "上海虹桥酒店",
    "roomTypeCode": "STDK",
    "roomTypeName": "标准大床房",
    "ratePlanCode": "BAR",
    "ratePlanName": "最优房价",
    "checkInDate": "2026-05-01",
    "checkOutDate": "2026-05-03",
    "nights": 2,
    "roomCount": 1,
    "adultCount": 2,
    "childCount": 0,
    "guestName": "张三",
    "guestPhone": "13800138000",
    "totalPrice": 1376.00,
    "currency": "CNY",
    "status": "active",
    "reservationStatus": "confirmed",
    "paymentStatus": "unpaid",
    "cancellationPolicy": {
      "type": "limited",
      "description": "入住前1天18:00前免费取消，之后扣首晚房费"
    },
    "createdAt": "2026-04-30T22:30:00+08:00"
  }
}
```

---

### 接口 5：支付订单

完成订单支付，更新支付状态。

- **方法**：`POST`
- **路径**：`/api/open/reservations/{reservationCode}/pay`
- **状态**：[ ] 未实现

#### 路径参数

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| reservationCode | String | 是 | 预订号 |

#### 请求体

```json
{
  "paymentMethod": "wechat_miniprogram",
  "paymentAmount": 1376.00,
  "transactionId": "WX202605010001"
}
```

#### 请求字段说明

| 字段 | 类型 | 必填 | 说明 |
|------|------|------|------|
| paymentMethod | String | 是 | 支付方式（见枚举） |
| paymentAmount | Decimal | 是 | 支付金额 |
| transactionId | String | 是 | 第三方支付流水号 |

#### 支付方式枚举

| 值 | 说明 |
|----|------|
| wechat_miniprogram | 微信小程序支付 |
| alipay | 支付宝 |
| credit_card | 信用卡 |
| bank_transfer | 银行转账 |

#### 业务规则

1. 校验预订号是否存在且 `status = active`
2. 校验 `payment_status` 当前为 `unpaid`，防止重复支付
3. 校验 `paymentAmount` 与订单 `total_price` 一致
4. 更新 `reservation.payment_status = paid`
5. 记录支付流水到 `reservation_history`

#### 响应数据

```json
{
  "code": 200,
  "message": "支付成功",
  "data": {
    "reservationCode": "CRS20260501000001",
    "paymentStatus": "paid",
    "paymentMethod": "wechat_miniprogram",
    "paymentAmount": 1376.00,
    "transactionId": "WX202605010001",
    "paidAt": "2026-05-01T10:05:00+08:00"
  }
}
```

---

## 三、安全与性能要求

### 安全要求

1. **传输安全**：所有接口必须使用 HTTPS
2. **认证机制**：通过 `X-Api-Key` + `X-Api-Secret` 双重校验，对应 `tenant_channels` 表
3. **数据隔离**：所有查询和操作严格按 `tenant_id` 隔离
4. **渠道授权**：酒店数据仅对已通过 `channel_hotel_mappings` 授权的渠道可见
5. **敏感数据**：信用卡信息需加密传输和存储
6. **频率限制**：单渠道每秒最大请求数 100

### 性能要求

| 接口 | 目标响应时间 |
|------|-------------|
| 查询酒店列表 | ≤ 500ms |
| 查询酒店详情与价格 | ≤ 800ms |
| 可订检查 | ≤ 300ms |
| 创建订单 | ≤ 1000ms |
| 支付订单 | ≤ 500ms |

---

## 四、数据模型依赖

本接口涉及的核心数据表及其关系：

```
hotels                      -- 酒店主表
├── hotel_images             -- 酒店图片
├── hotel_facilities         -- 酒店设施
├── hotel_room_types         -- 酒店房型
│   ├── room_type_facilities -- 房型设施（available=true 时对外展示）
│   └── hotel_prices         -- 房型每日价格（按 rate_plan + room_type + date）
├── rate_plans               -- 价格计划
│   ├── packages（JSON字段）  -- 包价信息直接存储在 rate_plans.packages JSON 列中
│   ├── cancellation_policies-- 取消政策（通过 cancellation_rule 关联）
│   └── guarantee_policies   -- 担保政策（通过 guarantee_rule 关联）
├── inventory                -- 库存（按 hotel + room_type + rate_plan + channel + date，独立表）
├── room_status              -- 房态（is_open 字段，无记录时默认开房）
├── booking_controls         -- 预订控制规则（日期级，优先于 rate_plans 中的规则）
├── channel_hotel_mappings   -- 渠道-酒店映射（控制渠道可见酒店）
└── reservation              -- 预订订单
```