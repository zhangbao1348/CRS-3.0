# CRS项目决策记录

> 本文档从项目聊天记录中提取的关键决策、规则定义和架构变更，按时间顺序整理。
> 生成日期：2025年

---

## 目录

1. [项目初始架构与前端创建](#1-项目初始架构与前端创建)
2. [PRD反向更新与需求文档规范](#2-prd反向更新与需求文档规范)
3. [集团房型实现方案设计](#3-集团房型实现方案设计)
4. [房价大类管理方案](#4-房价大类管理方案)
5. [SAAS租户隔离架构决策](#5-saas租户隔离架构决策)
6. [市场码大类管理实现方案](#6-市场码大类管理实现方案)
7. [市场码数据结构修正](#7-市场码数据结构修正)
8. [来源码管理功能完善](#8-来源码管理功能完善)
9. [渠道码按租户初始化](#9-渠道码按租户初始化)
10. [禁用自动数据库初始化](#10-禁用自动数据库初始化)
11. [用户角色权限初始化方案](#11-用户角色权限初始化方案)
12. [房型大类租户隔离完善](#12-房型大类租户隔离完善)
13. [前端UI控件隐藏与酒店模拟数据](#13-前端ui控件隐藏与酒店模拟数据)
14. [远程服务器部署方案](#14-远程服务器部署方案)
15. [集团房价码衍生层级管理](#15-集团房价码衍生层级管理)
16. [酒店房型实时查询](#16-酒店房型实时查询)
17. [酒店房价码按酒店查询](#17-酒店房价码按酒店查询)
18. [租户选择缓存与后端拦截机制](#18-租户选择缓存与后端拦截机制)
19. [文档同步更新核心原则](#19-文档同步更新核心原则)
20. [酒店数据按租户关联](#20-酒店数据按租户关联)
21. [租户ID获取规范化](#21-租户id获取规范化)
22. [Repository方法注解规范](#22-repository方法注解规范)
23. [租户维护与初始化数据](#23-租户维护与初始化数据)
24. [集团房价码数据规范](#24-集团房价码数据规范)

---


### [2025-04] 项目初始架构与前端创建
**来源：** 创建 CRS 高保真前端页面.md
**决策内容：** 确定CRS系统前端技术栈为React 18 + Ant Design 5 + Vite，采用前后端分离架构。前端页面包括酒店管理、集团管理、价格管理、渠道管理等核心模块。酒店管理页面移除了删除功能和税率列，优化了查询模块UI字体统一为14px。
**影响范围：** 整个前端项目结构、src/pages/ 下所有页面组件
**关键规则：**
- 前端使用React 18 + Ant Design 5 + Vite构建
- 后端使用Spring Boot 3.2 + JPA + MySQL 8
- 前端端口3001，后端端口8080
- 所有API调用通过 `src/utils/api.js` 统一管理，禁止使用绝对路径

---

### [2025-04] PRD反向更新与需求文档规范
**来源：** 更新PRD：前端交互与后端接口.md
**决策内容：** 根据前端页面交互和后端接口内容，反向更新PRD文档（requirements.md），从v2.1升级到v3.0。覆盖所有菜单功能（首页、订单管理、库存管理、集团管理10+子模块、酒店管理、价格管理、渠道管理、数据报表、系统设置、超管设置），详细描述所有新增/编辑页面。补充了超预订管理、集团促销管理、房型差价、人数差价、门市价日历等详细内容。
**影响范围：** `.kiro/specs/requirements.md`
**关键规则：**
- PRD使用用户故事（US）+ 验收标准（AC）格式
- 使用产品语言描述，减少技术语言
- 条件交互使用 `[条件]` 标注字段显示/隐藏规则
- 每个表单必须包含完整的字段表格（字段、必填、类型、说明、默认值）

---

### [2025-04] 集团房型实现方案设计
**来源：** 集团房型实现方案设计.md
**决策内容：** 确定集团房型采用「集团-酒店两级房型架构」，实现集团统一管理和酒店个性化配置的平衡。设计了完整的数据库表结构、后端分层架构和前端页面结构。
**影响范围：** group_room_types表、hotel_room_types表、group_room_type_hotel表、room_type_categories表、后端Entity/Repository/Service/Controller、前端GroupRoomType.jsx和AddGroupRoomType.jsx
**关键规则：**
- 集团房型表 `group_room_types`：包含room_type_category_id、max_occupancy、sort_order等字段
- 酒店房型表 `hotel_room_types`：关联group_room_type_id，支持酒店个性化配置
- 关联表 `group_room_type_hotel`：支持room_info_editable控制酒店是否可修改房型信息
- 房型大类表 `room_type_categories`：一级结构，支持按大类分类管理
- API设计：GET/POST/PUT/DELETE /api/group-room-types，POST /{id}/allocate 批量分配到酒店
- 集团房型更新时，已分配的酒店房型可自动同步（取决于room_info_editable设置）

---

### [2025-04] 房价大类管理方案
**来源：** 房价大类管理方案.md
**决策内容：** 创建rate_types表实现房价大类管理的完整CRUD功能。包含数据库迁移脚本、后端RateTypeService/RateTypeController、前端RateCategory.jsx页面。初始化8条示例数据（BAR最佳可用房价、CORP企业协议价、PROMO促销价、GROUP团队价、PACKAGE包价、LONGSTAY长住价、SEASONAL季节价、WEEKEND周末价）。
**影响范围：** rate_types表、RateType.java、RateTypeService.java、RateTypeController.java、RateCategory.jsx
**关键规则：**
- 表结构：id, code, name, description, sort_order, status, created_at, updated_at
- API：GET/POST/PUT/DELETE /api/rate-types，GET /api/rate-types/active
- 前端使用TreeManagement组件，设置showAddChild={false}只支持一级节点

---

### [2025-04] SAAS租户隔离架构决策
**来源：** 房价大类管理方案.md、市场码大类管理实现方案.md、完善渠道码功能并按租户隔离.md
**决策内容：** 确定CRS系统为SAAS平台，所有业务数据必须按租户（tenant_id）进行隔离。所有实体类添加tenantId字段，Repository添加按tenantId查询方法，Service层所有方法添加tenantId参数，Controller使用DEFAULT_TENANT_ID=1作为默认租户。移除code字段的unique约束，改为(tenant_id, code)组合唯一约束。
**影响范围：** 所有业务实体类、Repository、Service、Controller；rate_types、market_codes、market_code_categories、channel_codes、source_codes、room_type_categories等表
**关键规则：**
- 所有业务表必须包含 `tenant_id` 字段
- 唯一约束改为 `(tenant_id, code)` 组合唯一，不同租户可以有相同编码
- Service层所有方法必须接收tenantId参数
- Controller使用 `DEFAULT_TENANT_ID = 1` 作为默认租户（后续改为从TenantContext获取）
- 数据库索引：为tenant_id字段创建索引

---

### [2025-04] 市场码大类管理实现方案
**来源：** 市场码大类管理实现方案.md
**决策内容：** 创建market_code_categories表实现市场码大类管理。采用一级结构（不支持子节点），与房价大类和房型大类保持一致的实现模式。后端采用Service接口+ServiceImpl实现类的分层模式。
**影响范围：** market_code_categories表、MarketCodeCategory.java、MarketCodeCategoryService.java、MarketCodeCategoryServiceImpl.java、MarketCodeCategoryController.java、MarketCodeCategory.jsx
**关键规则：**
- 表结构：id, code, name, description, status, tenant_id, created_at, updated_at
- API：GET/POST/PUT/DELETE /api/market-code-categories，GET /check-code检查编码唯一性
- 初始化6条数据：DIRECT直接预订、OTA渠道、CORPORATE企业客户、TRAVEL_AGENCY旅行社、MEMBER会员预订、PROMO促销活动
- 前端使用TreeManagement组件，showAddChild={false}

---

### [2025-04] 市场码数据结构修正
**来源：** 市场码.md
**决策内容：** 修正市场码数据结构设计错误。原方案将分类放在market_code_categories表、市场码放在market_codes表，但前端和后端期望的是统一的树形结构。修正为：所有数据统一放在market_codes表中，分类作为根节点（parent_id=null, level=1），具体市场码作为子节点（level=2）。
**影响范围：** market_codes表数据结构、MarketCode.java、MarketCodeService相关逻辑
**关键规则：**
- 市场码采用统一树形结构，全部存储在 `market_codes` 表中
- 根节点（level 1）：分类（企业客户、旅行社、OTA、会员/直客等）
- 子节点（level 2）：具体的市场码，通过parent_id关联到分类
- 每个租户独立的市场码数据，通过tenant_id隔离

---

### [2025-04] 来源码管理功能完善
**来源：** 完善源码管理功能.md
**决策内容：** 完善来源码（SourceCode）管理功能，添加租户隔离支持。修改Entity添加tenantId字段，Repository添加按租户查询方法，Service和Controller添加租户隔离逻辑。
**影响范围：** SourceCode.java、SourceCodeRepository.java、SourceCodeService.java、SourceCodeServiceImpl.java、SourceCodeController.java、SourceCode.jsx
**关键规则：**
- 来源码位于菜单：集团管理 → 来源码管理
- 数据按租户隔离，所有查询方法添加tenantId参数
- Controller使用DEFAULT_TENANT_ID=1

---

### [2025-04] 渠道码按租户初始化
**来源：** 渠道码.md
**决策内容：** 为渠道码实现按租户批量插入功能。创建数据库迁移脚本使用存储过程为每个租户安全插入3级渠道码体系。后端新增批量创建API和默认初始化API。
**影响范围：** channel_codes表、ChannelCodeService.java、ChannelCodeServiceImpl.java、ChannelCodeController.java、migration_20260411_insert_channel_codes_per_tenant.sql
**关键规则：**
- 渠道码采用3级树形结构，按租户隔离
- 使用存储过程安全插入，INSERT IGNORE避免重复
- 新增API：POST /api/channel-codes/batch 批量创建、POST /api/channel-codes/init-default/{tenantId} 初始化默认渠道码
- 为5个租户（万豪、希尔顿、洲际、凯悦、雅高）分别插入完整渠道码体系

---

### [2025-04] 禁用自动数据库初始化
**来源：** 完善渠道码功能并按租户隔离.md
**决策内容：** 禁止在代码中自动执行数据库初始化或迁移。DatabaseMigration类注释掉@Component注解，移除CommandLineRunner接口实现，清空所有迁移方法。所有数据库操作改为通过终端手动执行SQL脚本。
**影响范围：** DatabaseMigration.java、项目规则文档
**关键规则：**
- **禁止**在代码中自动执行数据库初始化或迁移
- 所有数据库操作必须**通过终端手动执行SQL脚本**
- DatabaseMigration类已禁用（注释掉@Component注解），保留代码作为参考
- 迁移文件命名格式：`migration_YYYYMMDD_description.sql`
- 存放路径：`backend/src/main/resources/database/`
- 数据库密码：12345678

---

### [2025-04] 用户角色权限初始化方案
**来源：** 完善渠道码功能并按租户隔离.md
**决策内容：** 创建5个超级管理员用户（admin~admin5），密码统一为admin123（BCrypt加密），租户ID设为NULL表示超级管理员。超级管理员角色通过 `SELECT 1, id FROM menus` 获得所有菜单权限，data_scope设为'all'。修复了菜单初始化SQL中缺少created_at/updated_at字段的问题。补全了系统管理员角色的菜单关联（角色管理、菜单管理）。
**影响范围：** init_auth_data.sql、users表、roles表、user_roles表、role_menus表
**关键规则：**
- 超级管理员：tenant_id=NULL，data_scope='all'，拥有所有菜单权限
- 系统管理员：包含用户管理(43)、角色管理(44)、菜单管理(45)、集团设置(46)、自定义渠道设置(47)
- 所有INSERT语句必须包含created_at和updated_at字段，使用CURRENT_TIMESTAMP
- MySQL环境变量配置：MYSQL_HOME=/usr/local/mysql

---

### [2025-04] 房型大类租户隔离完善
**来源：** 编辑保存房价大类报错.md、房型管理.md
**决策内容：** 完善房型大类（RoomTypeCategory）的租户隔离功能。添加tenantId字段，修改Repository/Service/Controller支持按租户查询。修复了Controller返回格式问题（categoryCode→code, categoryName→title, id→key）。集团房型新增页面的房型大类下拉框改为只读取有效（active）数据。
**影响范围：** RoomTypeCategory.java、RoomTypeCategoryRepository.java、RoomTypeCategoryService.java、RoomTypeCategoryController.java、migration_20260406_add_tenant_id_to_room_type_categories.sql
**关键规则：**
- 房型大类API返回格式：{key: id(字符串), title: categoryName, code: categoryCode}
- /api/room-type-categories/group/{groupId} 只返回status='active'的有效数据
- 新增 /api/room-type-categories/check-code 接口检查编码唯一性
- 初始化6条数据：标准房STANDARD、大床房KING、双床房TWIN、套房SUITE、行政房EXECUTIVE、家庭房FAMILY

---

### [2025-04] 前端UI控件隐藏与酒店模拟数据
**来源：** 隐藏并切换酒店信息.md
**决策内容：** 暂时隐藏CRS系统切换和租户切换控件。在HotelContext中添加5个模拟酒店数据，默认选中第一个（北京环球影城大酒店 BJS001）。
**影响范围：** MainLayout.jsx、HotelContext.jsx
**关键规则：**
- 侧边栏系统切换Select组件：注释隐藏
- 顶部导航栏租户切换Select组件：注释隐藏
- 酒店切换控件保留，加载模拟数据作为后备方案
- 默认选中第一个酒店

---

### [2025-04] 远程服务器部署方案
**来源：** 隐藏并切换酒店信息.md、部署前端项目到远程服务器.md
**决策内容：** 确定前端部署到远程CentOS服务器，使用Nginx作为Web服务器。安装sshpass工具实现自动化SSH连接。Nginx配置使用try_files支持SPA路由。
**影响范围：** 远程服务器Nginx配置、项目规则文档
**关键规则：**
- 远程服务器：IP 117.72.168.234，CentOS，用户root
- 部署目录：/usr/share/nginx/html/crs-frontend
- Nginx配置：listen 80，try_files $uri $uri/ /index.html
- 构建命令：npm run build → 生成dist目录 → scp上传
- API代理：location /api → proxy_pass http://localhost:8080

---

### [2025-04] 集团房价码衍生层级管理
**来源：** 集团房价码.md
**决策内容：** 为集团房价码增加衍生层级管理功能。定义三级衍生结构：基础价格(basic)、一级衍生价(level1)、二级衍生价(level2)。实现完整的多条件筛选功能（9个筛选条件）。同步更新PRD文档。
**影响范围：** group_rate_codes表（新增derivative_level等字段）、GroupRateCodeService.java、GroupRateCodeController.java、GroupRateCode.jsx、AddGroupRateCode.jsx、requirements.md
**关键规则：**
- 衍生层级：basic(基础价格)、level1(一级衍生价)、level2(二级衍生价)
- 一级衍生房价码：父级只能是基础房价码
- 二级衍生房价码：父级只能是一级衍生房价码
- 新增API：GET /api/group-rate-codes/selectable-parents 获取可选父级房价码
- 筛选条件：房价码名称、代码、房价类别、市场码、来源码、类型、房价码类型、促销优惠、状态
- 新增字段：derivative_level、rate_category、market_code_id、source_code_id、discount、rounding、guarantee_rule、cancellation_rule等

---

### [2025-04] 酒店房型实时查询
**来源：** 酒店房型管理页面实时查询显示.md
**决策内容：** 实现酒店房型管理页面从hotel_room_types表实时查询显示。为HotelRoomType实体类添加多个前端需要的字段。创建数据库迁移脚本添加新字段。
**影响范围：** HotelRoomType.java、HotelRoomTypeController.java、HotelRoomTypeService.java、migration_20260413_add_hotel_room_types_fields.sql
**关键规则：**
- 新增字段：englishName、maxAdults、maxChildren、roomQuantity、area、floor、windowType、bedType、image
- API /api/hotel-room-types/hotel/{hotelId} 直接返回数据数组（非包装格式）
- 数据库迁移使用存储过程安全添加字段

---

### [2025-04] 酒店房价码按酒店查询
**来源：** 酒店房价码.md
**决策内容：** 实现价格计划页面根据右上角选择的酒店实时加载对应的房价码数据。创建RatePlanController提供完整的价格计划API。前端通过HotelContext获取当前选中的酒店ID。酒店房型分配功能添加租户验证，确保只能访问当前租户的酒店房型。集团房型查询也添加租户过滤。
**影响范围：** RatePlanController.java、api.js（新增ratePlanApi）、RatePlan.jsx、HotelRoomTypeService.java、GroupRoomTypeService.java
**关键规则：**
- 价格计划API：GET /api/rate-plans/hotel/{hotelId}
- 前端通过 useHotelContext() 获取当前酒店ID
- 酒店切换时自动刷新数据
- HotelRoomTypeService添加租户验证：检查酒店是否属于当前租户
- GroupRoomTypeService的getAllGroupRoomTypes添加租户过滤

---

### [2025-04] 租户选择缓存与后端拦截机制
**来源：** 酒店管理.md
**决策内容：** 实现完整的租户选择缓存和后端拦截机制。前端：超管用户显示租户选择下拉框，选择的租户ID缓存到localStorage（key: crs_selected_tenant）。后端：创建TenantInterceptor拦截器，从请求头X-Tenant-Id获取租户ID，设置到TenantContext的ThreadLocal中。
**影响范围：** TenantContext.jsx、tenantUtils.js、api.js（请求拦截器）、TenantInterceptor.java、TenantContext.java、WebConfig.java
**关键规则：**
- 前端租户ID传递方式：请求头 X-Tenant-Id + URL参数 tenantId
- 超管用户：显示租户选择下拉框，缓存到localStorage
- 普通用户：使用用户信息中自带的tenantId
- 后端TenantContext基于ThreadLocal实现，每个请求线程独立
- TenantInterceptor在请求完成后清理TenantContext，防止内存泄漏
- 酒店列表根据当前选择的租户查询

---

### [2025-04] 文档同步更新核心原则
**来源：** 酒店管理.md
**决策内容：** 确立核心开发原则：每次涉及到逻辑和代码的变更，必须同步更新需求文档和技术方案文档。禁止在未更新文档的情况下直接修改代码。
**影响范围：** 项目规则文档、开发流程
**关键规则：**
- **每次变更必须更新两个文件：**
  1. 需求文档 `.kiro/specs/requirements.md`：更新业务规则、功能描述和交互逻辑
  2. 技术方案文档 `.kiro/specs/design.md`：更新技术实现、API设计和架构变更
- 禁止在未更新文档的情况下直接修改代码
- 此为核心原则，写入项目规则

---

### [2025-04] 酒店数据按租户关联
**来源：** 酒店管理.md
**决策内容：** 确认酒店数据结构已与租户正确关联。Hotel实体类包含tenantId字段，HotelRepository提供findByTenantId()和findByTenantIdAndStatus()方法。酒店切换控件根据当前选择的租户查询对应酒店列表。
**影响范围：** Hotel.java、HotelRepository.java、HotelService.java
**关键规则：**
- Hotel实体包含tenantId字段
- 酒店列表查询必须按tenantId过滤
- 酒店切换控件联动租户选择

---

### [2025-04] 租户ID获取规范化
**来源：** Graphify Command.md
**决策内容：** 审查后端代码中租户ID获取方式，发现多个Controller使用硬编码DEFAULT_TENANT_ID=1而非TenantContext.getTenantId()。要求所有后端代码统一使用TenantContext.getTenantId()获取当前租户ID。
**影响范围：** 所有使用DEFAULT_TENANT_ID的Controller文件
**关键规则：**
- 所有后端代码必须通过 `TenantContext.getTenantId()` 获取租户ID
- 禁止使用硬编码 `DEFAULT_TENANT_ID = 1`
- TenantContext基于ThreadLocal，由TenantInterceptor在每次请求时设置

---

### [2025-04] Repository方法注解规范
**来源：** 预览项目.md
**决策内容：** 修复菜单分配保存时的500错误，原因是deleteByRoleId方法缺少@Modifying和@Transactional注解。确立Repository方法注解规范。
**影响范围：** 所有Repository中的自定义删除/更新方法
**关键规则：**
- 自定义删除/更新方法必须添加 `@Modifying` 和 `@Transactional` 注解
- 使用 `@Query` 显式指定JPQL或SQL语句
- 使用 `@Param` 绑定查询参数
- 示例：
  ```java
  @Modifying
  @Transactional
  @Query("DELETE FROM UserRole ur WHERE ur.userId = :userId")
  void deleteByUserId(@Param("userId") Integer userId);
  ```

---

### [2025-04] 租户维护与初始化数据
**来源：** 生成项目概要文档.md
**决策内容：** 在超管设置的租户新增/编辑页面添加"酒店数量"字段。创建Tenant实体类和租户维护SQL脚本，初始化租户基础信息。
**影响范围：** AddEditTenant.jsx、Tenant.java、租户初始化SQL
**关键规则：**
- 租户表单新增字段：hotelCount（酒店数量），数字输入框，必须≥0
- 租户初始化数据通过SQL脚本手动执行

---

### [2025-04] 集团房价码数据规范
**来源：** 集团房型.md
**决策内容：** 规范集团房价码测试数据，每个租户减少到20条。参考T1_RACK配置统一其他房价码的字段值（除代码和名称外保持一致）。为3个租户各插入20条房价码，包含标准价、周末价、企业价、会员价、团队价、促销价等20种类型。
**影响范围：** group_rate_codes表数据、migration_20260412_insert_20_group_rate_codes_per_tenant.sql
**关键规则：**
- 每个租户20条房价码，编码前缀区分租户（T1_/T2_/T3_）
- 统一关联：market_code_id=75, source_code_id=7
- 担保规则：ALIPAY_GUARANTEE
- 取消规则：HUAZHU_STANDARD
- 状态：active
